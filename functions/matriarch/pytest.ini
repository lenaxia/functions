[pytest]
testpaths = tests
python_files = test_*.py
# Collect only Test* classes and test_* functions (matches violetscans).
# The previous `python_*= *` rules accidentally treated helper functions
# as tests.
python_classes = Test*
python_functions = test_*
addopts = -v
